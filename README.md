# NC Plus One
NC Plus One is a Python and PostgreSQL project. It utilises relational databases modelling an events management company

## Technologies
- Python
- PostgreSQL

## Database Setup

To create a fresh local database, run:

```bash
psql -d postgres -f db/setup.sql